{% extends 'base.html.twig' %}
{# <h1>{{search}}</h1> #}

{% block body %}
	<div class="row">
		<div class="col-12 col-md-6">
			{% for comment in comments %}
				<div class="card mb-4">
					<div class="card-body p-3">
						{{comment.body}}
					</div>
				</div>
			{% endfor %}
		</div>
		<div class="col-12 col-md-6">
			{{form(form)}}
			</div>
		</div>
	{% endblock %}
